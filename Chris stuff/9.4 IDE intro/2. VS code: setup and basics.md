1. Why IDE

   - See slide

2. Setup layout

   - Share and open Imagineers_base.code-profile

3. Open the right folder

   - Open the correct project/root folder, not just one file
   - Understand what the Explorer is showing
   - Explain why agents work better when they can see the full project/work folder
   - Basic workspace structure:
     - client-or-project-name/
       - README.md (for humans)
       - AGENTS.md (for agents)
       - /inputs (raw source materials)
       - /reference (supporting context)
       - /working (drafts and work in progress)
       - /outputs (final deliverables)
       - Optional:
         - /instructions (task briefs)
         - /review (human review)
         - /logs (run and error logs)
         - /scripts (repeatable helpers)
   - Workspace trust

4. Learn the layout without getting overwhelmed

   - Explorer
   - Editor area
   - Agents
   - Terminal
   - Preview tab vs pinned tabs

5. Basic navigation inside a project

   - Quick Open (type filename in command palette)
   - Find in files across the project
   - Breadcrumbs
   - Outline view

6. Basic editing that immediately saves time

   - Search and replace in a file
   - Project-wide search and replace
   - Multi-cursor (Option-click, Alt-click)
   - Auto save (File > Autosave)

7. Files in and files out

   - Create files
   - Rename files
   - Move files
   - Explain why work should live in files, not just in chat
   - Explain how file-based workflows make chunking easier

8. Terminal basics in the IDE

   - What the integrated terminal is
   - How to open it
   - How to know what folder it is currently in
   - Switching between terminals

9. First agent workflow on a bounded task

   - How to point an agent at the right folder
   - How to give a small, bounded task
   - How to keep scope tight
   - When to use one agent vs another
   - Why the IDE gives better control than a chat app
   - Why files are better than long chat threads for handoff

10. Review what the agent changed

    - Create and view a diff
    - Compare before/after (open a changed non-commit file)
    - Do not trust outputs blindly

11. Git basics

    - Changed files in Source Control (white, green, yellow)
    - Stage/unstage changes
    - Commit changes to local git
    - Push/sync to GitHub

12. Professional working habits inside the IDE

    - See slide
